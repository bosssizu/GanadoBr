# GanadoBravo — Fullstack v4 (IA raza)
- `/` sirve `static/index.html`
- POST `/evaluate` con `file` + `mode` (levante|vaca_flaca|engorde)
- Raza por IA (OpenAI) si `OPENAI_API_KEY` está presente; si no, fallback local.
